{
  "name": "@flow-machine/built-in-tasks",
  "private": true,
  "version": "0.1.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@flow-machine/shared-types": "workspace:*",
    "@flow-machine/task-sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.2"
  }
}
